import {VerticalBox} from "std-widgets.slint";

export component ItMoss inherits Window {
    callback request-file-name();
    in-out property <string> file;
    init => {
        root.request-file-name()
    }
    VerticalBox {
        Text {
            text: "\{root.file}";
        }
    }
}